{
 "name": "entity.presentComments",
 "status": "documented",
 "tokens": "per official docs",
 "verified": "docs-only",
 "purpose": "Provide comments for work objects. Apps call this endpoint to send per-user flexpane comment data to the client.",
 "source": "docs.slack.dev / slack-ruby/slack-api-ref mirror",
 "params": {
  "comments": {
   "required": true,
   "type": "array",
   "desc": "Array of comments to present to the user. See the comment schema for the full list of properties."
  },
  "cursor": {
   "required": false,
   "type": "string",
   "desc": "App supplied cursor used for pagination, will be sent in the next request for comments."
  },
  "can_post_comment": {
   "required": false,
   "type": "boolean",
   "desc": "Indicates whether the user has permissions to post comments."
  },
  "trigger_id": {
   "required": true,
   "type": "string",
   "desc": "A reference to the original user action that initiated the request."
  },
  "delete_action_id": {
   "required": false,
   "type": "string",
   "desc": "The block action id that will be sent when a delete request is initiated for a comment."
  },
  "user_auth_required": {
   "required": false,
   "type": "boolean",
   "desc": "Set to true (or 1) to indicate that the user must authenticate to see the comments data."
  },
  "user_auth_url": {
   "required": false,
   "type": "string",
   "desc": "A custom URL to which users are directed for authentication if required."
  },
  "error": {
   "required": false,
   "type": "string",
   "desc": ""
  }
 },
 "params_known": true,
 "params_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev args)",
 "response_known": false,
 "response": null,
 "response_source": null,
 "write_shaped": false,
 "url": "https://vs.izie.top/redocumented/api/methods/entity.presentComments.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
