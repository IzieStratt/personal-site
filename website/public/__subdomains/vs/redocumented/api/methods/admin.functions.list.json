{
 "name": "admin.functions.list",
 "status": "documented",
 "tokens": "per official docs",
 "verified": "docs-only",
 "purpose": "Look up functions by a set of apps.",
 "source": "docs.slack.dev / slack-ruby/slack-api-ref mirror",
 "params": {
  "team_id": {
   "required": false,
   "type": "string",
   "desc": "The team context to retrieve functions from."
  },
  "app_ids": {
   "required": true,
   "type": "array",
   "desc": "Comma-separated array of app IDs to get functions for; max 50."
  },
  "include_non_distributed_functions": {
   "required": false,
   "type": "boolean",
   "desc": "Whether to also include functions that are not yet distributed to any users in the function list. This is needed for admins that are approving an app request and will only work if the team owns the app."
  },
  "cursor": {
   "required": false,
   "type": "string",
   "desc": "Set cursor to next_cursor returned by the previous call to list items in the next page."
  },
  "limit": {
   "required": false,
   "type": "integer",
   "desc": "The number of results that will be returned by the API on each invocation. Must be between 1 and 1000, both inclusive."
  }
 },
 "params_known": true,
 "params_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev args)",
 "response": {
  "ok": "true",
  "functions": "unknown[]",
  "response_metadata": "{",
  "next_cursor": "string"
 },
 "response_known": true,
 "response_source": "ImShyMike/slack-undoc-client (generated TS types, cross-referenced not independently verified)",
 "write_shaped": false,
 "url": "https://vs.izie.top/redocumented/api/methods/admin.functions.list.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
