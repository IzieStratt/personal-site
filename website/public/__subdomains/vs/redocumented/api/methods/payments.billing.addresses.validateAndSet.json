{
 "name": "payments.billing.addresses.validateAndSet",
 "status": "undocumented",
 "tokens": "unknown",
 "verified": "existence-only",
 "purpose": "(from internal-canvas cross-reference; see methods/internal-canvas-2026.md)",
 "source": "internal Canvas (private, hackclub workspace; bonus 4th source, user-linked mid-task)",
 "params": {
  "abn_id": {
   "required": false,
   "type": "string"
  },
  "city": {
   "required": false,
   "type": "string"
  },
  "company_name": {
   "required": false,
   "type": "string"
  },
  "country": {
   "required": false,
   "type": "string"
  },
  "is_business": {
   "required": false,
   "type": "boolean"
  },
  "is_vat_registered": {
   "required": false,
   "type": "boolean"
  },
  "state": {
   "required": false,
   "type": "string"
  },
  "street1": {
   "required": false,
   "type": "string"
  },
  "street2": {
   "required": false,
   "type": "string"
  },
  "vat_id": {
   "required": false,
   "type": "string"
  },
  "waiting_for_vat": {
   "required": false,
   "type": "boolean"
  },
  "zip": {
   "required": false,
   "type": "string"
  },
  "checkout_step": {
   "required": false,
   "type": "string"
  }
 },
 "params_known": true,
 "params_source": "ImShyMike/slack-undoc-client (generated TS types, cross-referenced not independently verified)",
 "response": {
  "ok": "true",
  "id": "number",
  "team_id": "number",
  "date_create": "number",
  "date_update": "number",
  "date_delete": "number",
  "fields": "{",
  "country_code": "string",
  "city": "string",
  "state": "string",
  "street1": "string",
  "street2": "string",
  "zip": "string",
  "date_validate": "null",
  "is_business": "boolean",
  "waiting_for_vat": "boolean",
  "is_vat_registered": "boolean",
  "country": "string",
  "notes": "null",
  "company_name": "string"
 },
 "response_known": true,
 "response_source": "ImShyMike/slack-undoc-client (generated TS types, cross-referenced not independently verified)",
 "write_shaped": false,
 "url": "https://vs.izie.top/redocumented/api/methods/payments.billing.addresses.validateAndSet.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
