{
 "name": "team.integrationLogs",
 "status": "documented",
 "tokens": "Live-tested 2026-09-20 with app_id filter, both team- and enterprise-scoped xoxc: not_allowed_token_type both times -- session tokens are categorically not permitted to call this one, independent of Grid scope (same failure class as chat.scheduleMessage in auth-and-tokens.md). Untested whether a real xoxb/xoxp app token with the right OAuth scope can reach it. Also note: the documented change_type enum is added/removed/enabled/disabled/updated -- not app_installed/app_enabled, which the method's install-detection use case might suggest by analogy to other admin.* naming. See methods/admin-write-scope-2026-09.md.",
 "verified": "docs-only (live-tested 2026-09-20, not_allowed_token_type on both token scopes)",
 "purpose": "Gets the integration logs for the current team.",
 "source": "docs.slack.dev / slack-ruby/slack-api-ref mirror",
 "params": {
  "app_id": {
   "required": false,
   "type": "string",
   "desc": "Filter logs to this Slack app. Defaults to all logs."
  },
  "change_type": {
   "required": false,
   "type": "enum",
   "desc": "Filter logs with this change type. Possible values are added, removed, enabled, disabled, and updated. Defaults to all logs."
  },
  "service_id": {
   "required": false,
   "type": "string",
   "desc": "Filter logs to this service. Defaults to all logs."
  },
  "team_id": {
   "required": false,
   "type": "string",
   "desc": "encoded team id to get logs from, required if org token is used."
  },
  "user": {
   "required": false,
   "type": "user",
   "desc": "Filter logs generated by this user's actions. Defaults to all logs."
  }
 },
 "params_known": true,
 "params_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev args)",
 "response_known": true,
 "response": null,
 "response_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev response example)",
 "response_example": "{\n    \"ok\": true\n}",
 "write_shaped": false,
 "url": "https://vs.izie.top/redocumented/api/methods/team.integrationLogs.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
