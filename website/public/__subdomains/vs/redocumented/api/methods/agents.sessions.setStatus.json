{
 "name": "agents.sessions.setStatus",
 "status": "documented",
 "tokens": "per official docs",
 "verified": "docs-only",
 "purpose": "Set an agent session's lifecycle status, creating the session if needed.",
 "source": "docs.slack.dev / slack-ruby/slack-api-ref mirror",
 "params": {
  "channel_id": {
   "required": false,
   "type": "string",
   "desc": "ID of the channel containing the agent session. Required for public channels."
  },
  "status": {
   "required": true,
   "type": "enum",
   "desc": "The lifecycle status to set."
  },
  "thread_ts": {
   "required": false,
   "type": "string",
   "desc": "Timestamp of the thread root message the session is scoped to. Required for thread-based sessions in regular channels and DMs. Must be omitted for session channels."
  },
  "title": {
   "required": false,
   "type": "string",
   "desc": "Title for the agent session (max 200 characters). Only used when creating a new session; ignored if the session already exists. To rename an existing session, use agents.sessions.rename."
  },
  "initiator_user_id": {
   "required": false,
   "type": "string",
   "desc": "The user who initiated the session. Only used when creating a new session; ignored if the session already exists. Must be a member of the channel."
  },
  "icon_emoji": {
   "required": false,
   "type": "string",
   "desc": "Emoji to use as the agent's icon. Takes priority over icon_url. Remains in effect until you clear it (pass null) or set a new value. Requires the chat:write.customize scope."
  },
  "icon_url": {
   "required": false,
   "type": "string",
   "desc": "URL to an image to use as the agent's icon. Remains in effect until you clear it (pass null) or set a new value. Requires the chat:write.customize scope."
  },
  "username": {
   "required": false,
   "type": "string",
   "desc": "Display name override for the agent (max 200 characters). Remains in effect until you clear it (pass null) or set a new value. Requires the chat:write.customize scope."
  }
 },
 "params_known": true,
 "params_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev args)",
 "response_known": true,
 "response": null,
 "response_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev response example)",
 "response_example": "{\n    \"ok\": true,\n    \"status\": \"processing\",\n    \"agent_status\": \"processing\",\n    \"title\": \"Scuba diving research\"\n}",
 "write_shaped": true,
 "url": "https://vs.izie.top/redocumented/api/methods/agents.sessions.setStatus.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
