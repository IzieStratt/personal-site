{
 "name": "admin.apps.permissions.set",
 "status": "documented",
 "tokens": "per official docs",
 "verified": "docs-only",
 "purpose": "Set the permission type for who can access an app",
 "source": "docs.slack.dev / slack-ruby/slack-api-ref mirror",
 "params": {
  "app_id": {
   "required": true,
   "type": "string",
   "desc": "Encoded ID of the app."
  },
  "permission_type": {
   "required": true,
   "type": "enum",
   "desc": "The type of permission that defines who can access the app."
  },
  "user_ids": {
   "required": false,
   "type": "array",
   "desc": "List of user IDs to allow for named_entities visibility."
  },
  "usergroup_ids": {
   "required": false,
   "type": "array",
   "desc": "List of encoded usergroup IDs."
  },
  "channel_restriction_mode": {
   "required": false,
   "type": "enum",
   "desc": "The mode that defines where the app can be used in channels."
  },
  "channel_ids": {
   "required": false,
   "type": "array",
   "desc": "List of encoded channel IDs for channel restrictions. Semantics depend on channel_restriction_mode: allowlist for specific_channels, exclusion list for all_channels_except."
  }
 },
 "params_known": true,
 "params_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev args)",
 "response_known": true,
 "response": null,
 "response_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev response example)",
 "response_example": "{\n    \"ok\": true,\n    \"permission_type\": \"everyone\",\n    \"channel_restriction_mode\": \"specific_channels\",\n    \"channel_ids\": [\n        \"C00000001\",\n        \"C00000002\"\n    ]\n}",
 "write_shaped": true,
 "url": "https://vs.izie.top/redocumented/api/methods/admin.apps.permissions.set.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
