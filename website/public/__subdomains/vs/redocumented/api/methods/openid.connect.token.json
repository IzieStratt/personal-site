{
 "name": "openid.connect.token",
 "status": "documented",
 "tokens": "per official docs",
 "verified": "docs-only",
 "purpose": "Exchanges a temporary OAuth verifier code for an access token for Sign in with Slack.",
 "source": "docs.slack.dev / slack-ruby/slack-api-ref mirror",
 "params": {
  "client_id": {
   "required": false,
   "type": "string",
   "desc": "Issued when you created your application."
  },
  "client_secret": {
   "required": false,
   "type": "string",
   "desc": "Issued when you created your application."
  },
  "code": {
   "required": false,
   "type": "string",
   "desc": "The code param returned via the OAuth callback."
  },
  "redirect_uri": {
   "required": false,
   "type": "string",
   "desc": "This must match the originally submitted URI (if one was sent)."
  },
  "grant_type": {
   "required": false,
   "type": "enum",
   "desc": "The grant_type param as described in the OAuth spec."
  },
  "refresh_token": {
   "required": false,
   "type": "string",
   "desc": "The refresh_token param as described in the OAuth spec."
  },
  "code_verifier": {
   "required": false,
   "type": "string",
   "desc": "PKCE code verifier (RFC 7636). Required when the authorization request included a code_challenge."
  }
 },
 "params_known": true,
 "params_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev args)",
 "response_known": true,
 "response": null,
 "response_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev response example)",
 "response_example": "{\n    \"ok\": true,\n    \"access_token\": \"xoxp-1234\",\n    \"token_type\": \"Bearer\",\n    \"id_token\": \"eyJhbGcMjY5OTA2MzcWNrLmNvbVwvdGVhbV9p...\"\n}",
 "write_shaped": false,
 "url": "https://vs.izie.top/redocumented/api/methods/openid.connect.token.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
