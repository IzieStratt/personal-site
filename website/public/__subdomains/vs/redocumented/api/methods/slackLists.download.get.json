{
 "name": "slackLists.download.get",
 "status": "documented",
 "tokens": "per official docs",
 "verified": "docs-only",
 "purpose": "Retrieve List download URL from an export job to download List contents.",
 "source": "docs.slack.dev / slack-ruby/slack-api-ref mirror",
 "params": {
  "list_id": {
   "required": true,
   "type": "string",
   "desc": "ID of the List to export."
  },
  "job_id": {
   "required": true,
   "type": "string",
   "desc": "The ID of the recently started job to export the List."
  },
  "format": {
   "required": false,
   "type": "string",
   "desc": "Format the export was started with. Must match the format passed to slackLists.download.start. Defaults to csv."
  },
  "include_threads": {
   "required": false,
   "type": "boolean",
   "desc": "Must match the include_threads passed to slackLists.download.start. The returned download_url carries this through so the served export matches what was generated. Only applies when format is json."
  },
  "include_attachments": {
   "required": false,
   "type": "boolean",
   "desc": "Must match the include_attachments passed to slackLists.download.start. The returned download_url carries this through so the served export matches what was generated. Only applies when format is json."
  }
 },
 "params_known": true,
 "params_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev args)",
 "response_known": true,
 "response": null,
 "response_source": "slack-ruby/slack-api-ref (mirrors official docs.slack.dev response example)",
 "response_example": "{\n    \"ok\": true,\n    \"status\": \"COMPLETED\",\n    \"download_url\": \"https://files.com/files-pri/1234567890-F12345678/csv/list?origin_team=T1234567890\"\n}",
 "write_shaped": false,
 "url": "https://vs.izie.top/redocumented/api/methods/slackLists.download.get.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
