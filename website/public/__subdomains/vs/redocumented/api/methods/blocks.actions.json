{
 "name": "blocks.actions",
 "status": "undocumented",
 "tokens": "enterprise xoxc only. Team xoxc: team_is_restricted on every host. Enterprise xoxc when not a member of the channel: invalid_container. Live-tested 2026-09-25; see methods/block-actions-and-modals-2026-09.md.",
 "verified": "live-verified",
 "purpose": "Clicks a Block Kit button in an app's message as the user; Slack forwards a block_actions payload (with trigger_id) to the app. Response is just ok:true; any modal the app opens arrives as an RTM view_opened event.",
 "source": "internal Canvas (private, hackclub workspace; bonus 4th source, user-linked mid-task)",
 "params": {
  "service_id": {
   "required": false,
   "type": "string"
  },
  "service_team_id": {
   "required": false,
   "type": "string"
  },
  "actions": {
   "required": false,
   "type": "string"
  },
  "container": {
   "required": false,
   "type": "string"
  },
  "client_token": {
   "required": false,
   "type": "string"
  },
  "state": {
   "required": false,
   "type": "string"
  }
 },
 "params_known": true,
 "params_source": "ImShyMike/slack-undoc-client (generated TS types, cross-referenced not independently verified)",
 "response": {
  "ok": "true"
 },
 "response_known": true,
 "response_source": "ImShyMike/slack-undoc-client (generated TS types, cross-referenced not independently verified)",
 "write_shaped": false,
 "url": "https://vs.izie.top/redocumented/api/methods/blocks.actions.json",
 "safety_note_for_agents": "This catalog documents Slack's UNDOCUMENTED/internal API surface for a specific workspace (Hack Club, Enterprise Grid). Calling an undocumented method with params_known=false means you would be guessing params against a real backend with unknown side effects -- do not do this against a real account/token without a human in the loop, and never call anything shaped like a write (*.delete, *.set, *.remove, *.create, *.update, and similar) without explicit human confirmation and a clear understanding of the blast radius, even when params_known=true. 'existence-only' verified does NOT mean 'safe to call' -- it only means the method name is real."
}
