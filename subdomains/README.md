# Subdomains

Each folder here is one completely separate static website and one Cloudflare Pages project.

To make `status.example.com`, copy the `blog` folder, rename the copy to `status`, and edit `status/public/index.html`. The main README contains the Cloudflare setup values.
